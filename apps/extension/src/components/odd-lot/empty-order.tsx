// import { SearchX } from "lucide-react";
// import type { FC } from "react";

// export const EmptyOrdersState: FC<{ type: "all" | "my" }> = ({ type }) => {
// 	const message = ` ${type === "all" ? "No orders found for today or please recheck your search company name." : "My orders are empty."} `;

// 	return (
// 		<div className="flex flex-col items-center justify-center h-full p-6 text-gray-400">
// 			<div className=" rounded-lg p-8 text-center max-w-sm mx-auto shadow-xl">
// 				<SearchX
// 					className="h-16 w-16 text-blue-500 mx-auto mb-5"
// 					strokeWidth={1.5}
// 				/>
// 				<h3 className="text-xl font-semibold text-gray-200 mb-2">
// 					{type === "all" ? "No Orders Found" : "My Orders"}
// 				</h3>
// 				<p className="text-sm">{message}</p>
// 			</div>
// 		</div>
// 	);
// };
