// import type { TopState } from '@/state/top-state'
// import type { stateResult } from '@/types/misc-types'
// import type { DashboardData } from '@/types/top-types'

// export const selectTopData = (state: TopState): DashboardData => state.topData
// export function selectSetTopData(state: TopState): ((data: DashboardData) => void) {
//   return state.setTopData
// }
// export const selectIsTopDataLoading = (state: TopState): boolean => state.isTopDataLoading
// export function selectFetchTopData(state: TopState): (() => Promise<stateResult>) {
//   return state.fetchTopData
// }
