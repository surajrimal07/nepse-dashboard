// import type { RateLimitState } from '@/state/rate-limit-state'
// import type { RateLimit } from '@/types/rate-limit-types'

// export const selectRateLimit = (state: RateLimitState): RateLimit => state.rateLimit
// export function selectSetRateLimit(state: RateLimitState): ((rateLimit: RateLimit) => void) {
//   return state.setRateLimit
// }

// export const selectRateLimitSuccess = (state: RateLimitState): boolean => state.rateLimit.success
